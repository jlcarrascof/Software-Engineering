const names = ["Rebecca", "Rex", "Priyanka", "Rory", "Kai", "Lana"];

// Printing odd indexed names

for (let i = 0; i < names.length; i++) {
    if (i % 2 === 1) {
        console.log(names[i]);
    }
}
